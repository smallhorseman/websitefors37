{
  "name": "workflow",
  "version": "1.0.0",
  "main": "expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "dev": "expo start",
    "build": "echo 'Expo build - use EAS'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  }
}
