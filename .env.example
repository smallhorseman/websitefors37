# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# Required for server-only API routes that use supabaseAdmin (admin auth/session)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: protect /api/setup-admin in production
# If set, requests must include ?token=<value> or header x-admin-setup-token
ADMIN_SETUP_TOKEN=

# Google Gemini AI Configuration (optional)
GEMINI_API_KEY=your_gemini_api_key
